#include <iostream>
#include <stdio.h>
#include <windows.h>

#include "IFileInfo.h"


int main(int argc, const char ** argv)
{
    WIN32_FIND_DATA FindFileData;
    HANDLE hFind;

    IFileInfo ** FindFiles(const char* aFolder);
}